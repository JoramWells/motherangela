export const Sidebar = ({ children }: { children: React.ReactNode }) => {
  return (
    <div
      className="bg-sky-800
    h-screen
    fixed
    w-64

    "
    >
      {children}
    </div>
  );
};
