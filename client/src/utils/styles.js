/* eslint-disable no-unused-vars */
export const selectStyles = {
  control: (provided, state) => ({
    ...provided,
    minHeight: '40px',
    height: '40px',
  }),
  input: (provided) => ({
    ...provided,
  }),
};
